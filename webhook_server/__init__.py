"""
webhook_server — FastAPI server that receives A2A push notification deliveries.

Demonstrates F4 (Push Notifications): acts as the webhook endpoint that
async_agent POSTs TaskStatusUpdateEvents to.
"""
