[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    eval: marks tests as evaluation harness tests (slow)
    integration: marks tests requiring real GCP credentials
    unit: marks pure unit tests (no network, no GCP)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
