"""
pipeline_agent — SequentialAgent coordinating a 3-stage research pipeline.

Demonstrates F10 (SequentialAgent workflow pattern) and F13 (session state
passing between pipeline stages via context.state).
"""
